
func fibo(n: int): int{
     if(n <= 1){
     	  return 1;
     }
     
     var val: int = fibo(n - 1) + fibo(n - 2);

     return val;
}

func main(): void {
     for(var i:int =0; i<30; i++){
          print(fibo(i), "\n");
     }
}