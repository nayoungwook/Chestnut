
class inheritance implement.

class attribute id system with inheritance implement.

func call & constructor parameter type checking.

assignable check.

Array parser implementation.

new, return statement.

IR generation.

in new statement, we have to consider memory allocation size and data align.
